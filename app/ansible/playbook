---
- hosts: all
  #gather_facts: False
  tasks:
  - name: return motd to registered var
    shell: hostname
#    register: myhostname
#
#
#  - name: display Host Variable from hostfile
#    debug: msg=" The {{ inventory_hostname }} Value is {{ myhostname.stdout }}"
...
